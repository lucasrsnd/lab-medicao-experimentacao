[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lab02"
version = "0.1.0"
description = "Pacotes compartilhados do Lab02 - Assistentes de IA vs. Codificação Manual"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["config"]

[tool.setuptools.packages.find]
include = ["src*"]
